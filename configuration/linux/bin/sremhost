#!/usr/bin/env python

import sys

def usage():
    print "usage: %s server ip(re)" % sys.argv[0]
    print "   eg: %s {170|180|10} 1.18 [--dry]" % sys.argv[0]
    sys.exit(-1)

def list_items():
    pass

def del_items():
    pass

if len(sys.argv) != 2:
    usage()
